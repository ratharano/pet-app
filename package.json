{
  "name": "sanctuary-pet",
  "version": "1.0.0",
  "description": "Sanctuary Pet — feed on time, watch your companion grow",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "start": "vite --port 3000",
    "preview": "vite preview --port 3000",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android"
  },
  "license": "ISC",
  "devDependencies": {
    "@capacitor/cli": "^7.4.2",
    "serve": "^14.2.4"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2",
    "@capacitor/ios": "^7.4.2",
    "@capacitor/preferences": "^7.0.4",
    "vite": "^8.0.16"
  }
}
